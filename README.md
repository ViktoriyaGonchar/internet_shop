# 🚢 SHOP SHIPS - Интернет-магазин (Flask Web App)

**Проектная тема:** Интернет-магазин с веб-интерфейсом и локальной CRM

**Среда разработки:** PyCharm  
**Язык:** Python 3.8+  
**Фреймворк:** Flask 3.0+  
**Frontend:** Bootstrap 5.3+

## 🎯 Описание проекта

Современный интернет-магазин с веб-интерфейсом на Flask, демонстрирующий:
- Модульную архитектуру с применением принципов **SOLID** и **DRY**
- ООП дизайн с использованием абстракций и репозиториев
- Красивый и интуитивно понятный пользовательский интерфейс на Bootstrap
- Полный набор CRUD-операций для товаров и заказов
- Разделение на публичную часть и CRM (админ-панель)

## 🔑 Ключевые возможности

### Публичная часть (покупатель)
- 📦 Просмотр каталога товаров с красивыми карточками
- 🔍 Поиск товаров по названию или описанию
- 🛒 Управление корзиной (добавление, изменение количества, удаление)
- 💰 Автоматический пересчёт общей суммы корзины
- 📋 Оформление заказа с подтверждением
- 💾 Сохранение корзины между сессиями

### Локальная CRM (админ-панель)
- 🔐 Простая авторизация (пароль: `admin`)
- ➕ Добавление новых товаров через веб-форму
- ✏️ Редактирование товаров (название, описание, цена, статус наличия)
- 🗑️ Удаление товаров с подтверждением
- 📋 Просмотр истории заказов
- 🔍 Просмотр деталей каждого заказа
- 📊 Статистика магазина (количество товаров, заказов, выручка, средний чек)

## 🏗️ Архитектура проекта

Проект следует принципам **SOLID** и **DRY**:

### SOLID принципы:
- **Single Responsibility** - каждый класс имеет одну ответственность (репозитории, сервисы)
- **Open/Closed** - открыт для расширения через интерфейсы (`IStorage`)
- **Liskov Substitution** - реализации интерфейсов взаимозаменяемы
- **Interface Segregation** - маленькие специализированные интерфейсы
- **Dependency Inversion** - зависимость от абстракций (`IStorage`), а не от конкретных реализаций

### DRY принципы:
- Базовый класс `BaseService` для общих методов сервисов
- Переиспользуемые функции (`get_cart_service()`, декораторы)
- Единый базовый шаблон `base.html`

### Структура проекта:

```
internet_shop/
├── app.py                 # Flask приложение (точка входа для веб)
├── main.py                # Консольная версия (старая)
├── data_manager.py        # Менеджер данных (использует репозитории)
├── models/                # Модели данных
│   ├── product.py
│   ├── cart.py
│   └── order.py
├── storage/               # Хранилище (абстракция)
│   ├── base_storage.py    # Интерфейс IStorage
│   └── json_storage.py    # Реализация JSON хранилища
├── repositories/          # Репозитории (Repository Pattern)
│   ├── product_repository.py
│   ├── order_repository.py
│   └── cart_repository.py
├── services/              # Бизнес-логика (сервисы)
│   ├── base_service.py    # Базовый класс (DRY)
│   ├── cart_service.py
│   ├── product_service.py
│   └── order_service.py
├── templates/             # HTML шаблоны (Jinja2)
│   ├── base.html          # Базовый шаблон
│   ├── public/           # Публичные страницы
│   │   ├── index.html
│   │   ├── product_detail.html
│   │   ├── search.html
│   │   ├── cart.html
│   │   └── order_success.html
│   ├── admin/            # Админ-панель
│   │   ├── login.html
│   │   ├── dashboard.html
│   │   ├── products.html
│   │   ├── product_form.html
│   │   ├── orders.html
│   │   └── order_detail.html
│   └── errors/           # Страницы ошибок
│       ├── 404.html
│       └── 500.html
├── static/               # Статические файлы
│   ├── css/
│   │   └── style.css     # Кастомные стили
│   └── js/
│       └── main.js       # JavaScript функции
├── data/                 # JSON-файлы (создаётся автоматически)
│   ├── products.json
│   ├── orders.json
│   └── cart.json
├── requirements.txt      # Зависимости Python
└── README.md            # Этот файл
```

## 🚀 Установка и запуск

### Требования
- Python 3.8 или выше
- pip (менеджер пакетов Python)

### Шаги для запуска

1. **Клонирование проекта**:
   ```bash
   git clone <repository-url>
   cd internet_shop
   ```

2. **Создание виртуального окружения** (рекомендуется):
   ```bash
   python -m venv venv
   ```
   
   Активация виртуального окружения:
   - **Windows**: `venv\Scripts\activate`
   - **Linux/Mac**: `source venv/bin/activate`

3. **Установка зависимостей**:
   ```bash
   pip install -r requirements.txt
   ```

4. **Запуск веб-приложения**:
   ```bash
   python app.py
   ```
   
   Или через Flask:
   ```bash
   flask run
   ```

5. **Откройте в браузере**:
   ```
   http://localhost:5000
   ```

### Доступ к админ-панели

1. Перейдите по адресу: `http://localhost:5000/admin/login`
2. Введите пароль: `admin`
3. Получите доступ к CRM панели управления

## 📖 Использование

### Публичная часть

1. **Главная страница** (`/`) - каталог товаров с карточками
2. **Поиск** (`/search?q=...`) - поиск товаров
3. **Детали товара** (`/product/<id>`) - информация о товаре
4. **Корзина** (`/cart`) - управление корзиной
5. **Оформление заказа** - создание заказа из корзины

### CRM (Админ-панель)

1. **Панель управления** (`/admin`) - статистика и обзор
2. **Управление товарами** (`/admin/products`) - CRUD операции
3. **Управление заказами** (`/admin/orders`) - просмотр заказов
4. **Детали заказа** (`/admin/orders/<id>`) - подробная информация

## 💾 Хранение данных

Все данные хранятся в JSON-файлах в директории `data/`:
- `products.json` — каталог товаров
- `orders.json` — история заказов
- `cart.json` — текущая корзина

> При первом запуске директория `data/` создаётся автоматически.

## 🛠️ Технические детали

### Применённые паттерны:
- **Repository Pattern** - абстракция доступа к данным
- **Service Layer** - бизнес-логика отделена от представления
- **Dependency Injection** - внедрение зависимостей через конструкторы
- **Template Inheritance** - переиспользование шаблонов (Jinja2)

### Frontend:
- **Bootstrap 5.3** - современный и адаптивный дизайн
- **Bootstrap Icons** - красивые иконки
- **Кастомные стили** - анимации, hover-эффекты
- **JavaScript** - валидация форм, UX улучшения

### Backend:
- **Type hints** - аннотации типов во всех модулях
- **Docstrings** - документация для всех классов и методов
- **Обработка ошибок** - 404, 500 страницы
- **Flash сообщения** - уведомления пользователю

## 🔮 Возможности для расширения

Проект спроектирован с учётом будущего масштабирования:

- ✅ Замена `JSONStorage` на реальную БД (SQLite, PostgreSQL, MySQL)
- ✅ Добавление полноценной системы аутентификации (Flask-Login)
- ✅ REST API для мобильных приложений
- ✅ Интеграция с платёжными системами
- ✅ Система уведомлений (email, SMS)
- ✅ Расширенная аналитика и отчёты
- ✅ Многоязычность (i18n)
- ✅ Кэширование (Redis)
- ✅ Файловое хранилище для изображений товаров

## 📝 Лицензия

См. файл `LICENSE` в корне проекта.

## 👨‍💻 Автор Виктория Гончар

Проект создан в рамках учебного задания для демонстрации:
- Навыков работы с Python и Flask
- Модульной архитектуры приложений
- Применения принципов SOLID и DRY
- Создания современного веб-интерфейса

## 🎨 Скриншоты

### Публичная часть:
- Красивый каталог товаров с карточками
- Удобная корзина с автоматическим подсчётом
- Детальные страницы товаров

### CRM:
- Современная панель управления
- Удобные формы редактирования
- Детальная статистика

---
Made by Viktoriya Gonchar

