# 🚢 SHOP SHIPS - Интернет-магазин

**Проектная тема:** Интернет-магазин (Базовая структура + Локальная CRM)

**Среда разработки:** PyCharm  
**Язык:** Python 3.8+

## 🎯 Описание проекта

Интернет-магазин с модульной архитектурой, демонстрирующий работу со сложными структурами данных и реализацию CRUD-операций. Проект имитирует клиент-серверное взаимодействие на стороне бэкенда с использованием хранения данных в памяти и JSON-файлах.

## 🔑 Ключевые возможности

### Публичная часть (покупатель)
- 📦 Просмотр каталога товаров (название, описание, цена, наличие)
- 🔍 Поиск товаров по названию или описанию
- 🛒 Добавление/удаление товаров в корзину
- 💰 Автоматический пересчёт общей суммы корзины
- 📋 Оформление заказа (сохранение текущего состояния корзины как заказа)
- 💾 Сохранение корзины между запусками приложения

### Локальная CRM (админ-панель)
- ➕ Добавление новых товаров
- ✏️ Редактирование товаров (название, описание, цена, статус наличия)
- 🗑️ Удаление товаров
- 📋 Просмотр истории заказов
- 🔍 Просмотр деталей каждого заказа
- 📊 Статистика магазина (количество товаров, заказов, выручка, средний чек)

## 🧱 Архитектура проекта

Проект организован по модульному принципу с чётким разделением на слои:

```
internet_shop/
├── models/              # Модели данных
│   ├── __init__.py
│   ├── product.py      # Класс Product
│   ├── cart.py         # Класс Cart
│   └── order.py        # Класс Order
├── storage/             # Хранилище данных
│   ├── __init__.py
│   └── json_storage.py # Работа с JSON-файлами
├── services/            # Бизнес-логика
│   ├── __init__.py
│   ├── cart_service.py
│   ├── product_service.py
│   └── order_service.py
├── ui/                  # Пользовательский интерфейс
│   ├── __init__.py
│   ├── public_ui.py    # Интерфейс для покупателей
│   └── crm_ui.py       # Интерфейс для администраторов
├── data/                # JSON-файлы с данными (создаётся автоматически)
│   ├── products.json
│   ├── orders.json
│   └── cart.json
├── data_manager.py      # Менеджер данных (центральная логика)
├── main.py             # Точка входа в приложение
├── requirements.txt    # Зависимости проекта
└── README.md          # Этот файл
```

### Компоненты архитектуры

- **`models/`** — Классы данных (`Product`, `Order`, `Cart`) с методами сериализации/десериализации
- **`storage/`** — Абстракция для работы с файловой системой (JSON-файлы)
  > *В будущем можно легко заменить на реальный API-клиент или ORM-модель*
- **`services/`** — Бизнес-логика (работа с корзиной, товарами, заказами)
- **`ui/`** — Пользовательский интерфейс (консольные меню)
- **`data_manager.py`** — Центральный менеджер, инкапсулирующий всю логику работы с данными
  > *Служит точкой входа для замены на реальный бэкенд*

## 🚀 Установка и запуск

### Требования
- Python 3.8 или выше
- PyCharm (рекомендуется) или любой другой Python IDE

### Шаги для запуска

1. **Клонирование проекта** (если используется Git):
   ```bash
   git clone <repository-url>
   cd internet_shop
   ```

2. **Создание виртуального окружения** (рекомендуется):
   ```bash
   python -m venv venv
   ```
   
   Активация виртуального окружения:
   - Windows: `venv\Scripts\activate`
   - Linux/Mac: `source venv/bin/activate`

3. **Установка зависимостей** (на данный момент не требуются):
   ```bash
   pip install -r requirements.txt
   ```
   > Примечание: проект использует только стандартную библиотеку Python, поэтому зависимости отсутствуют.

4. **Запуск приложения**:
   ```bash
   python main.py
   ```

## 📖 Использование

### Публичная часть (режим покупателя)

1. При запуске выберите пункт **"1. Публичная часть (покупатель)"**
2. В главном меню доступны следующие опции:
   - **Просмотр каталога** — список всех доступных товаров
   - **Поиск товаров** — поиск по названию или описанию
   - **Корзина** — просмотр и управление корзиной
   - **Оформить заказ** — создание заказа из текущей корзины

### CRM (режим администратора)

1. При запуске выберите пункт **"2. CRM (администратор)"**
2. В главном меню доступны следующие опции:
   - **Управление товарами** — CRUD-операции с товарами
   - **Просмотр заказов** — история всех заказов
   - **Статистика** — общая статистика магазина

## 💾 Хранение данных

Все данные хранятся в JSON-файлах в директории `data/`:
- `products.json` — каталог товаров
- `orders.json` — история заказов
- `cart.json` — текущая корзина (сохраняется между сессиями)

> При первом запуске директория `data/` создаётся автоматически.

## 🛠️ Технические детали

- **Type hints** — все функции и методы используют аннотации типов
- **Docstrings** — все классы и методы документированы
- **Обработка ошибок** — базовые проверки на корректность данных
- **Модульность** — код разделён на логические модули для лёгкого масштабирования

## 🔮 Возможности для расширения

Проект спроектирован с учётом будущего масштабирования:

- Замена `JSONStorage` на реальную базу данных (SQLite, PostgreSQL)
- Переход на веб-интерфейс с использованием Flask/FastAPI
- Добавление системы аутентификации и авторизации
- Реализация REST API для клиент-серверного взаимодействия
- Добавление системы уведомлений
- Интеграция с платёжными системами

## 📝 Лицензия

См. файл `LICENSE` в корне проекта.

## 👨‍💻 Автор

Проект создан в рамках учебного задания для демонстрации навыков работы с Python, модульной архитектурой и CRUD-операциями.
