# Быстрый старт 🚀

## Установка и запуск

1. **Установите зависимости**:
   ```bash
   pip install -r requirements.txt
   ```

2. **Запустите приложение**:
   ```bash
   python app.py
   ```

3. **Откройте в браузере**:
   ```
   http://localhost:5000
   ```

## Первый запуск

1. Откройте главную страницу: `http://localhost:5000`
2. Для входа в админ-панель: `http://localhost:5000/admin/login`
   - Пароль: `admin`
3. Добавьте несколько товаров через админ-панель
4. Начните совершать покупки!

## Структура URL

### Публичная часть:
- `/` - главная страница (каталог)
- `/search?q=...` - поиск товаров
- `/product/<id>` - детали товара
- `/cart` - корзина

### Админ-панель:
- `/admin/login` - вход
- `/admin` - панель управления
- `/admin/products` - управление товарами
- `/admin/orders` - управление заказами

---



